/**
 * AI Bot user ID - always show first in user lists
 */
export const AI_BOT_ID = "ikem-ai-bot";

// ============================================
// UI Constants
// ============================================

/**
 * Available reaction emoji options
 */
export const REACTION_OPTIONS = ["👍", "👎", "❣️", "❤️", "😂", "😮", "😢", "😡"] as const;

/**
 * Swipe gesture threshold in pixels
 */
export const SWIPE_THRESHOLD = 72;

/**
 * Swipe action button width in pixels
 */
export const ACTION_WIDTH = 64;
